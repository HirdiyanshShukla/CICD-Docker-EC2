apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: django-app-ns

patches:
  - path: patch-ingress.yml
  - path: patch-deployment.yml # 👈 ADD THIS LINE
